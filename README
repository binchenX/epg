An demostration of using ContentProvider to provide EPG database.

Notes: after install the apk ,use following command to install the sample database file


adb push asserts/epg.db /data/system/epg.db
